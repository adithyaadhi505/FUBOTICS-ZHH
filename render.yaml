services:
  - type: web
    name: fubotics-zhh
    env: static
    rootDir: web
    buildCommand: npm install && npm run build
    staticPublishDir: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
